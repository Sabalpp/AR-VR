# Tunnel terminates public TLS; only loopback port 8080 is published by Compose.
:8080 {
    handle /api/* {
        reverse_proxy backend:8000
    }
    handle {
        reverse_proxy frontend:3000
    }
}
